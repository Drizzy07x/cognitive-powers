# Release queue workflow

Using the authorized real browser host:

1. Focus **Search tickets**, enter `QCU-217`, and press `Enter`.
2. Open the visible `QCU-217` result.
3. Set **Assignee** to `Maya Chen`.
4. Set **Priority** to `High`.
5. Set **Release note** to `Validated in staging`.
6. Activate **Save details**, then **Mark ready**, in that order.
7. Observe the completed ticket from a fresh browser.

The objective is complete only when the visible status is `Ready`, all three
saved values match, the ordered public interaction log is complete, and the
fresh observation agrees with the action receipt.
