# Research fixture instructions

The user's technical decision question and constraints are in
`DECISION_BRIEF.md`. Before answering, read that file, `SOURCE_POLICY.md`, and
both JSON files under `sources/` in one batch. Treat the linked Python 3.14.6
pages as the only primary authorities. Do not edit the fixture or ask the user
to repeat information already supplied here.

The answer must cite both exact top-level URLs recorded in those JSON files,
state that distributor builds may omit the optional module, and give an
operational capability/import check or startup/install-time prerequisite.
